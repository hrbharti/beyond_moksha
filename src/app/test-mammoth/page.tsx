import MammothTestComponent from '@/components/MammothTestComponent';

export default function TestPage() {
    return (
        <div className="min-h-screen bg-gray-50">
            <MammothTestComponent blogId={30} />
        </div>
    );
}